# Catch the cat

## Python lessons / LESSON #2

### установка пакетов:

```
// for windows
// открыть terminal и набрать команды:

easy_install pip
pip install pillow
```

### домашнее задание

```
по клику выбирать одно из 6 направлений и 
перемещать фигуру на новое положение

для этого: 

1) написать функцию move_pos в модуле helper
p_new = move_pos(p, d)
которая вычисляет и возвращает новую позицию p 
по заданному направлению d 

2) сделать метод класса фигуры move
  def move(self)
  - в методе выбирать случайно 1 из 6 направлений
  - вызывать функцию move_pos для вычисления нового положения

3) вызвать метод figure.move из метода on_click класса App  

справка:
направления
 1 - вверх-вперед
 2 - вперед
 3 - вниз-вперед
 4 - вниз-назад
 5 - назад
 6 - вверх-назад

подсказка:
  посмотреть как сделана аналогичная функция в проекте pac-man 
```


